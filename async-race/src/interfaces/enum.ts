export enum EngineStatus {
  start = 'started',
  stop = 'stopped',
  drive = 'drive'
}

export enum WinnersSort {
  id = 'id',
  wins = 'wins',
  time = 'time'
}

export enum SortOrder {
  asc = 'ASC',
  desc = 'DESC'
}
